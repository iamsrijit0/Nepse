name: Daily ML Trading Signals

on:
  schedule:
    # Run every day at 6:00 PM NPT (12:15 UTC)
    - cron: '15 12 * * *'
  workflow_dispatch:  # Allow manual trigger

jobs:
  generate-signals:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      with:
        token: ${{ secrets.GH_TOKEN }}
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install numpy pandas statsmodels requests


    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pandas numpy requests scikit-learn
      
    
    - name: Run ML Signal Generator
      env:
        GH_TOKEN: ${{ secrets.GH_TOKEN }}
      run: |
        python daily_ml_runner_github.py
    
    - name: Commit and push if changes
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add -A
        git diff --quiet && git diff --staged --quiet || (git commit -m "🤖 Automated ML signals update - $(date +'%Y-%m-%d')" && git push)
